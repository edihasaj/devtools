@page "/json-string-encoder"
@using System.Text.Json

<h1>JSON String Encoder</h1>

<textarea class="form-control code" @bind="text"></textarea>

<div>
    <button class="btn btn-primary" @onclick="Encode">Encode to JSON string</button>
    <button class="btn btn-primary" @onclick="Decode">Decode JSON string</button>
</div>

@if (result != null)
{
    <pre class="result">@result</pre>
}

@code {
    string text = "";
    string result;

    void Encode()
    {
        try
        {
            result = JsonSerializer.Serialize(text);
        }
        catch (Exception ex)
        {
            result = ex.Message;
        }
    }

    void Decode()
    {
        try
        {
            result = JsonSerializer.Deserialize<object>(text)?.ToString();
        }
        catch (Exception ex)
        {
            result = ex.Message;
        }
    }

}